SELECT AlertId, SensorId, Status, OpenedAt,
       AdjustedAtOpen, MinAtOpen, MaxAtOpen,
       SnoozedUntil, Message
FROM AlertLog
WHERE Status IN ('OPEN','ACK')
  AND (SnoozedUntil IS NULL OR SnoozedUntil < NOW())
ORDER BY OpenedAt DESC;
